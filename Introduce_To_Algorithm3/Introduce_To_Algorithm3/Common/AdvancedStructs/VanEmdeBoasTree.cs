using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Introduce_To_Algorithm3.Common.AdvancedStructs
{
    /// <summary>
    /// this runs at O(lglgn). The limit is that the keys must be integers in the range 0 and n-1, with no duplidates allowed.
    /// we will use n to denote the number of elements
//currently in the set and u as the range of possible values, so that each van Emde
//Boas tree operation runs in O(lg lg u) time. We call the set f(0; 1; 2; .... ; u)
//the universe of values that can be stored and u the universe size. We assume
//throughout this chapter that u is an exact power of 2,  for some integer
    /// </summary>
    public class VanEmdeBoasTree<V>
    {
    }
}
